# e02-validate.yml (steps only)
on:
  pull_request:
    paths-ignore: ["**/*.md"]
  workflow_dispatch:

steps:
  - uses: actions/checkout@v4
  - uses: databricks/setup-cli@v1.13.0
  - name: Validate the dev target
    run: databricks bundle validate -t dev
